<h1>These are your groups <%= @user.name %>!</h1>
<ul>
    <% @memberships.each do |mem| %>
        <li><%= link_to mem.group.name, group_path(mem.group)%></li>
    <% end %>
</ul>
<h4><strong><%= button_to "Create a new group!", new_group_path, method: :get %></strong><h4>