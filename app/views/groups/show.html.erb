<h1><%= @group.name %></h2>
<p><strong>Description: </strong><%= @group.description %>

<h2>Members List</h2>
<ul>
    <% @group.users.each do |user| %>
        <li><%= link_to user.name, user_path(user) %></li>
    <% end %>
</ul>